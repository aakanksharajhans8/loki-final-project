# Server Configuration
server.port=8083

# Application Name
spring.application.name=quotation-service

# Database Configuration (MySQL inside Docker network)
spring.datasource.url=${DB_URL:jdbc:mysql://mysql:3306/quotation_db?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USER:quotation_user}
spring.datasource.password=${DB_PASSWORD:secret}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Actuator Endpoints
management.endpoints.web.exposure.include=health,info,prometheus

# SpringDoc OpenAPI/Swagger UI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
